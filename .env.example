# Puerto de acceso a la aplicación
APP_PORT=8080

# Secreto JWT — genera uno con: openssl rand -hex 32
JWT_SECRET=cambia-este-secreto-en-produccion

# Rutas del host para datos persistentes
CARPETAS_PATH=./carpetas
PLANTILLAS_PATH=./plantillas
